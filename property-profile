<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Starbucks Grand Island | Executive Property Dashboard</title>
  <style>
    :root {
      --brand-primary: #1E3A5F;
      --brand-secondary: #2E5A87;
      --brand-accent: #4A90D9;
      --brand-success: #22C55E;
      --brand-warning: #F59E0B;
      --brand-danger: #EF4444;
      --brand-light: #F8FAFC;
      --brand-dark: #0F172A;
      --brand-gray: #64748B;
      --brand-border: #E2E8F0;
    }

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: system-ui, -apple-system, 'Segoe UI', Roboto, sans-serif;
      background: #F1F5F9;
      min-height: 100vh;
      color: var(--brand-dark);
    }

    .dashboard-container {
      max-width: 1400px;
      margin: 0 auto;
      padding: 24px;
    }

    /* Header */
    .dashboard-header {
      background: linear-gradient(135deg, var(--brand-primary) 0%, var(--brand-secondary) 100%);
      border-radius: 16px;
      padding: 32px;
      margin-bottom: 24px;
      color: white;
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      flex-wrap: wrap;
      gap: 24px;
    }

    .header-brand {
      font-size: 11px;
      opacity: 0.8;
      letter-spacing: 1.5px;
      margin-bottom: 8px;
      text-transform: uppercase;
    }

    .header-title {
      font-size: 36px;
      font-weight: 700;
      margin-bottom: 8px;
    }

    .header-address {
      font-size: 16px;
      opacity: 0.9;
      margin-bottom: 16px;
    }

    .header-badges {
      display: flex;
      gap: 12px;
      flex-wrap: wrap;
    }

    .header-badge {
      background: rgba(255,255,255,0.15);
      padding: 6px 12px;
      border-radius: 20px;
      font-size: 12px;
      display: flex;
      align-items: center;
      gap: 6px;
    }

    .badge-dot {
      width: 6px;
      height: 6px;
      border-radius: 50%;
      background: var(--brand-success);
    }

    .tier-container {
      text-align: center;
    }

    .tier-circle {
      width: 110px;
      height: 110px;
      border-radius: 50%;
      background: rgba(255,255,255,0.1);
      border: 4px solid var(--brand-success);
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      margin-bottom: 8px;
      box-shadow: 0 4px 20px rgba(0,0,0,0.2);
    }

    .tier-code {
      font-size: 26px;
      font-weight: 700;
    }

    .tier-label {
      font-size: 10px;
      opacity: 0.7;
      text-transform: uppercase;
    }

    .tier-descriptor {
      font-size: 14px;
      font-weight: 600;
      color: var(--brand-success);
    }

    /* Navigation */
    .nav-tabs {
      display: flex;
      gap: 8px;
      margin-bottom: 24px;
      background: white;
      padding: 8px;
      border-radius: 12px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.06);
      flex-wrap: wrap;
    }

    .nav-tab {
      padding: 12px 20px;
      border: none;
      background: transparent;
      border-radius: 8px;
      font-size: 14px;
      font-weight: 500;
      cursor: pointer;
      transition: all 0.2s;
      color: var(--brand-gray);
    }

    .nav-tab:hover {
      background: var(--brand-light);
      color: var(--brand-dark);
    }

    .nav-tab.active {
      background: var(--brand-primary);
      color: white;
    }

    .tab-content {
      display: none;
      animation: fadeIn 0.3s ease;
    }

    .tab-content.active {
      display: block;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(10px); }
      to { opacity: 1; transform: translateY(0); }
    }

    /* Cards */
    .panel {
      background: white;
      border-radius: 12px;
      padding: 24px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.08);
      margin-bottom: 24px;
    }

    .panel-title {
      font-size: 18px;
      font-weight: 600;
      color: var(--brand-dark);
      margin-bottom: 20px;
      display: flex;
      align-items: center;
      gap: 10px;
    }

    .panel-title-badge {
      padding: 4px 12px;
      border-radius: 20px;
      font-size: 11px;
      font-weight: 600;
    }

    /* Grid Layouts */
    .grid-6 {
      display: grid;
      grid-template-columns: repeat(6, 1fr);
      gap: 16px;
    }

    .grid-4 {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 16px;
    }

    .grid-3 {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 24px;
    }

    .grid-2 {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 24px;
    }

    @media (max-width: 1200px) {
      .grid-6 {
        grid-template-columns: repeat(3, 1fr);
      }
      .grid-4 {
        grid-template-columns: repeat(2, 1fr);
      }
    }

    @media (max-width: 900px) {
      .grid-6, .grid-4, .grid-3, .grid-2 {
        grid-template-columns: 1fr;
      }
      .dashboard-header {
        flex-direction: column;
        align-items: center;
        text-align: center;
      }
    }

    /* KPI Cards */
    .kpi-card {
      background: white;
      border-radius: 12px;
      padding: 20px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.08);
      border-left: 4px solid var(--brand-gray);
      transition: transform 0.2s, box-shadow 0.2s;
    }

    .kpi-card:hover {
      transform: translateY(-2px);
      box-shadow: 0 4px 16px rgba(0,0,0,0.12);
    }

    .kpi-card.high { border-left-color: var(--brand-success); }
    .kpi-card.moderate { border-left-color: var(--brand-warning); }
    .kpi-card.low { border-left-color: var(--brand-danger); }

    .kpi-title {
      font-size: 11px;
      font-weight: 600;
      color: var(--brand-gray);
      text-transform: uppercase;
      letter-spacing: 0.5px;
      margin-bottom: 8px;
    }

    .kpi-value {
      font-size: 28px;
      font-weight: 700;
      color: var(--brand-dark);
      margin-bottom: 4px;
    }

    .kpi-subtitle {
      font-size: 12px;
      color: var(--brand-gray);
      margin-bottom: 8px;
    }

    .kpi-trend {
      display: inline-flex;
      align-items: center;
      gap: 4px;
      font-size: 13px;
      font-weight: 600;
      padding: 4px 10px;
      border-radius: 6px;
    }

    .kpi-trend.positive {
      background: rgba(34,197,94,0.1);
      color: var(--brand-success);
    }

    .kpi-trend.negative {
      background: rgba(239,68,68,0.1);
      color: var(--brand-danger);
    }

    .kpi-trend.neutral {
      background: rgba(100,116,139,0.1);
      color: var(--brand-gray);
    }

    /* Category Scores */
    .category-row {
      display: grid;
      grid-template-columns: 12px 200px 1fr 50px;
      gap: 12px;
      align-items: center;
      padding: 10px 0;
      border-bottom: 1px solid var(--brand-border);
    }

    .category-row:last-child {
      border-bottom: none;
    }

    .status-dot {
      width: 10px;
      height: 10px;
      border-radius: 50%;
    }

    .status-dot.high { background: var(--brand-success); }
    .status-dot.moderate { background: var(--brand-warning); }
    .status-dot.low { background: var(--brand-danger); }

    .category-name {
      font-size: 14px;
      color: var(--brand-dark);
    }

    .progress-track {
      height: 8px;
      background: var(--brand-border);
      border-radius: 4px;
      overflow: hidden;
    }

    .progress-fill {
      height: 100%;
      border-radius: 4px;
      transition: width 0.8s ease-out;
    }

    .progress-fill.high {
      background: linear-gradient(90deg, var(--brand-success), #4ade80);
    }

    .progress-fill.moderate {
      background: linear-gradient(90deg, var(--brand-warning), #fbbf24);
    }

    .progress-fill.low {
      background: linear-gradient(90deg, var(--brand-danger), #f87171);
    }

    .category-score {
      font-size: 14px;
      font-weight: 600;
      text-align: right;
    }

    /* Trade Area Bubbles */
    .bubble-container {
      position: relative;
      height: 420px;
      background: linear-gradient(180deg, #e8f4f8 0%, #d4e8ec 100%);
      border-radius: 12px;
      overflow: hidden;
    }

    .bubble-grid {
      position: absolute;
      inset: 0;
      background-image:
        linear-gradient(rgba(30,58,95,0.08) 1px, transparent 1px),
        linear-gradient(90deg, rgba(30,58,95,0.08) 1px, transparent 1px);
      background-size: 40px 40px;
    }

    .bubble-road-v {
      position: absolute;
      left: 45%;
      top: 0;
      width: 10%;
      height: 100%;
      background: rgba(100,116,139,0.15);
    }

    .bubble-road-h {
      position: absolute;
      top: 42%;
      left: 0;
      width: 100%;
      height: 8%;
      background: rgba(100,116,139,0.15);
    }

    .road-label {
      position: absolute;
      font-size: 10px;
      color: var(--brand-gray);
      font-weight: 500;
    }

    .zip-bubble {
      position: absolute;
      border-radius: 50%;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      cursor: pointer;
      transition: transform 0.3s, box-shadow 0.3s;
      color: white;
      font-weight: 600;
      text-shadow: 0 1px 3px rgba(0,0,0,0.3);
    }

    .zip-bubble:hover {
      transform: scale(1.15);
      box-shadow: 0 8px 30px rgba(0,0,0,0.3);
      z-index: 10;
    }

    .zip-bubble.selected {
      box-shadow: 0 0 0 4px white, 0 0 0 8px var(--brand-accent);
    }

    .bubble-code {
      font-size: 11px;
      opacity: 0.9;
    }

    .bubble-pct {
      font-size: 14px;
      font-weight: 700;
    }

    /* Metric Rows */
    .metric-row {
      dis
